Model parameters initial analysis

Model parameters roughly computed via method of moments.

3 model categories: for frequency (freq), severity (sev) and aggregate.

**For frequency:**
* freq.Initx: for x=3, 4, 5, 6 latent classes

**For severity:**
* sev.Initx: for x=4, 6, 8 latent classes

**For aggregate:**
* Initx: for x=3, 4, 5, 6 latent classes
